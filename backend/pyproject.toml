[project]
name = "dashboard-agile-2026-backend"
version = "1.0.0"
description = "FastAPI backend for Dashboard Agile 2026"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
